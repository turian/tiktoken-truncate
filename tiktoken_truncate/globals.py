"""Global values, essentially the OpenAI models currently tested + supported."""

# A dictionary mapping models to their maximum tokens
model_max_tokens = {
    # "text-embedding-3-large": 8192,
    # Temporary change for testing
    "text-embedding-3-large": 16,
    # Add other models and their corresponding max tokens here
}
